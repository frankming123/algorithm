#include <cstdio>
#include <iostream>

using namespace std;

int t, n, m;

int nums[1000000];

int main() {
    scanf("%d", &t);
    while (t--) {
        scanf("%d", &n);
        for (int i = 0; i < n; i++)
            scanf("%d", &nums[i]);
        

        scanf("%d",&m);
    }
    return 0;
}