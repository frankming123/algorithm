#include <cstring>
#include <iostream>

using namespace std;

int n, len;
char str[101];

int main() {
    scanf("%d", &n);
    while (n--) {
        scanf("%s", str);
        printf("%s\n", "OH YES!");
    }
    return 0;
}