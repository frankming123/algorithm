#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> matrix;
int maxn,len;

void dfs(int r,int c,)

int main() {
    int a, b;
    while (cin >> a >> b)
        cout << (a + b) << endl;
    return 0;
}